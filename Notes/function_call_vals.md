Local variables -> above frame pointer (with the caller)

return address -> below frame pointer (with the function call)

frame pointer -> either

spilt registers -> either (below probably makes more sense)

return value -> below

parameters -> below

